apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: kiali
spec:
  parentRefs:
    - name: trial-studio-gateway
      namespace: istio-system
  hostnames: ["kiali.trial.studio"]
  rules:
    - backendRefs:
        - name: kiali
          port: 20001